interface:
  display_name: "Agentic Kanban"
  short_description: "Develop, review, spec-drive, rebrand & package the Agentic Kanban VS Code extension"
  default_prompt: "Use $agentic-kanban when working on the vscode-agentic-kanban extension - lane/checklist workflow, /spec spec-driven development, /goal planning, /loop stage drivers, /work prompts, /doctor diagnostics, /evidence recording, AGENTS.md managed sentinels, Agent Kanban to Agentic Kanban branding, and the lint/test/build/VSIX release pipeline."

policy:
  allow_implicit_invocation: true
